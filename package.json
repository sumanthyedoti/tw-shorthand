{
  "private": true,
  "license": "MIT",
  "description": "A tailwind plugin that defines shorthand for classes where possible",
  "main": "src/index.js",
  "keywords": [
    "tailwind",
    "tailwind plugin",
    "tailwind shorthand"
  ],
  "author": "Sumanth Yedoti",
  "repository": "https://github.com/sumanthyedoti/tw-shorthand",
  "scripts": {
    "examples:dev": "pnpm --filter with-* dev",
    "examples:build": "pnpm --filter with-* build",
    "examples:serve": "pnpm --filter with-* serve",
    "shorthand:dev": "pnpm --filter with-shorthand dev",
    "playground:build": "pnpm --filter playground build"
  }
}
