{
  "private": true,
  "license": "MIT",
  "description": "A tailwind plugin that defines shorthand for classes where possible",
  "main": "src/index.js",
  "keywords": [
    "tailwind",
    "tailwind plugin",
    "tailwind shorthand"
  ],
  "author": "Sumanth Yedoti",
  "repository": "https://github.com/sumanthyedoti/tw-shorthand",
  "scripts": {
    "example:start": "pnpm --filter with-* start",
    "example:build": "pnpm --filter with-* build",
    "example:serve": "pnpm --filter with-* serve",
    "playground:build": "pnpm --filter playground build"
  }
}
